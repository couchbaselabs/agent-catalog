[tool.poetry]
name = "agentc"
version = "0.1.0"
description = "User facing repository for all things agent-catalog (tooling around agent workflows)."
repository = "https://github.com/couchbaselabs/rosetta"
authors = [
    "Glenn Galvizo <glenn.galvizo@couchbase.com>"
]
readme = "README.md"

[tool.poetry.urls]
Homepage = "https://couchbase.com"
Documentation = "https://docs.couchbase.com"
Repository = "https://github.com/couchbaselabs/rosetta"

[tool.poetry.scripts]
agentc = "agentc.__main__:main"

[tool.poetry.dependencies]
python = "^3.12"
pydantic-settings = "^2.4.0"

agent-catalog-core = { git = "git@github.com:couchbaselabs/rosetta-core.git" }
agent-catalog-lc = { git = "git@github.com:couchbaselabs/rosetta-lc.git", optional = true }

# For developers that want to use a local version of Rosetta.
# agent-catalog-core = { path = ".../rosetta-core", develop = true }
# agent-catalog-lc = { path = ".../rosetta-lc", develop = true, optional = true }

[tool.poetry.extras]
langchain = ["agent-catalog-lc"]

[tool.poetry.group.dev.dependencies]
pre-commit = "^3.0.0"

[tool.ruff]
line-length = 120
lint.select = ["F", "B", "I", "SIM"]
show-fixes = true

[tool.ruff.lint.isort]
no-sections = true
force-single-line = true
lines-between-types = 1
force-wrap-aliases = true
split-on-trailing-comma = false

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
